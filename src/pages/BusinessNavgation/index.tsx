import React, { useState } from 'react';
import { Button, Card, Space, Input } from 'antd';
import BaseProduct from './components/BaseProduct';
import { SearchOutlined } from '@ant-design/icons';

interface Props {}
const BusinessNavgation: React.FC<Props> = props => {
  return <BaseProduct />;
};
export default BusinessNavgation;
